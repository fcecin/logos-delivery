name: 'Check build health'
description: 'Assert that build configuration reaches the compiler'

runs:
  using: 'composite'
  steps:
    # Reads the make database. No Nim, no Nimble, no dependencies, no network.
    - name: Check build health
      shell: bash
      run: ./scripts/check_build_health.sh
